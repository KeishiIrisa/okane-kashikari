# okane-kashikari Frontend

Flutter アプリ (iOS / Android)。端末IDベースでログインなしで貸し借りを管理します。

## セットアップ

```bash
flutter pub get
```

## 実行

```bash
flutter run
```

## 環境

- `lib/core/api.dart` で API ベースURLを設定（環境変数または定数）。
